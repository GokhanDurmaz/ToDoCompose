<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.WorkSpace" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.App.Splash" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Splash screen background -->
        <item name="android:windowBackground">@color/white</item>
        <!-- Splash screen icon -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/todo_app_icon_for_android__1_</item>
        <!-- Splash screen animation duration -->
        <item name="windowSplashScreenAnimationDuration">166</item>
        <!-- Splash screen background -->
        <item name="windowSplashScreenBackground">@color/white</item>
    </style>
</resources>